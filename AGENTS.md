# Farm Game AI 架构与编码约束

## 1. 适用范围与目标

本文件适用于仓库根目录及其全部子目录。所有 AI 编码代理在分析、修改和验证本项目时都必须遵守本文件。

本项目采用 Godot 4 和 GDScript，核心开发原则如下：

- 可实例化的角色、物体、掉落物和交互设施使用独立场景封装。
- 实体优先通过场景和组件组合能力，不通过深层继承堆叠功能。
- 可复用行为提取为公共组件，场景专属行为与所属场景放在一起。
- 角色复杂行为通过通用状态机框架和角色专属状态实现。
- 新代码严格遵守目标规范；旧代码只在任务涉及的范围内渐进修正。

规则中的“必须”“禁止”是强制约束，“应”表示除非存在明确理由，否则必须采用的默认做法。

## 2. 开始任务前

开始修改前必须：

1. 阅读 `project.godot` 和与任务最接近的已有场景、脚本及组件。
2. 检查工作区状态，区分用户已有修改与本次任务修改。
3. 查找同类实现并沿用其架构模式，不能在不了解现有实现时新建平行体系。
4. 确认资源路径、NodePath、信号连接、碰撞层和 InputMap action 等关联项。
5. 将修改限制在任务范围内；发现无关问题时记录说明，不顺手进行大规模整理。

用户未提交的修改属于用户。禁止覆盖、回退、删除或重新格式化与当前任务无关的修改。

## 3. 目录职责

### 3.1 资源与场景

- `assets/`：图片、调色板及其他源美术资源。不要在业务脚本中复制或生成同类资源。
- `scenes/characters/<character_name>/`：角色独立场景、角色控制脚本和角色专属状态。
- `scenes/objects/<category>/`：树、石头、掉落物等世界对象的独立场景和专属脚本。
- `scenes/houses/`：门、房屋及其他建筑场景。
- `scenes/components/`：可组合、可复用的公共组件场景与脚本。
- `scenes/test/`：针对角色、对象、地图或交互的最小手工测试场景。
- `tilesets/`：Godot TileSet 资源；对应源图片仍位于 `assets/`。
- `shader/`：共享 Shader。实体只配置 Shader 参数，不复制 Shader 实现。

### 3.2 脚本

- `scripts/state_machine/`：与具体角色无关的状态机框架。
- `scripts/globals/`：确实需要跨模块共享的枚举、数据类型或常量。
- `scripts/` 根目录及其他子目录：跨场景服务和通用逻辑。
- 只服务于一个场景的脚本必须与该场景就近存放，不得为了“统一”全部移动到 `scripts/`。

禁止重新引入遗留拼写 `charaters`。新增角色场景统一使用 `scenes/characters/`。除非任务明确要求迁移，否则不要批量重命名仍被引用的旧资源目录。

## 4. 独立场景优先

以下内容必须封装为独立 `.tscn` 场景：

- 可在地图中放置的角色或对象。
- 可在运行时生成的掉落物、作物、动物或交互设施。
- 会在两个及以上父场景中重复使用的节点组合。
- 拥有自身动画、碰撞、状态或生命周期的实体。

独立场景必须满足：

- 根节点代表完整实体，并使用稳定、明确的英文 `PascalCase` 节点名。
- 场景内部拥有自身动画、碰撞和组件，外部场景不复制其内部节点。
- 外部只通过根节点的导出属性、公共方法和信号交互，不依赖深层子节点路径。
- 场景必须能单独实例化；不得隐式依赖测试场景中的特定父节点或兄弟节点。
- 运行时生成实体时，使用带类型的 `PackedScene` 引用：常量资源可用 `const ...: PackedScene = preload(...)`，可配置资源使用 `@export var ...: PackedScene`。
- 实例化后先设置必要数据和位置，再添加到职责明确的父节点；不要复制粘贴目标场景的节点结构。

新增内容时优先参考：

- 玩家角色及其状态：`scenes/characters/player/`。
- 可破坏对象：`scenes/objects/trees/`、`scenes/objects/rocks/`。
- 可复用交互能力：`scenes/components/` 下的 Hit、Hurt、Damage、Interactable 和 Collectable 组件。

学习这些实现的组合思想和职责划分，不复制其中的拼写错误、空模板函数、重复分支或调试输出。

## 5. 公共组件提取

满足下列任一条件的行为应提取为公共组件：

- 已被两个及以上实体使用。
- 虽暂时只有一个调用方，但职责独立、边界清晰，并确定会由其他实体复用。
- 需要独立配置、碰撞检测、信号输出或生命周期管理。

以下逻辑通常保留在实体脚本中：

- 只属于单个实体且依赖其具体动画、节点布局或剧情流程的行为。
- 很短、没有独立状态且尚无第二个使用方的逻辑。
- 只有通过大量实体专属条件才能抽象的行为。

组件规则：

- 文件统一命名为 `<capability>_component.gd` 和 `<capability>_component.tscn`。
- 脚本类型使用 `<Capability>Component`，并按需要声明 `class_name`。
- 使用满足职责的最轻量根节点：无空间能力时使用 `Node`；需要区域检测时使用 `Area2D`；不要无理由使用 `Node2D`。
- 通过带类型的 `@export` 属性接收配置或依赖。
- 通过带类型参数的信号输出事件，例如 `signal hurt(damage: int)`。
- 不硬编码具体角色、实体、父节点名称、测试场景或资源路径。
- 不通过连续 `get_parent()` 或深层 `$"../../..."` 获取外部依赖。
- 组件负责通用机制；实体脚本负责组合组件、播放专属动画、生成专属掉落物和处理实体生命周期。

继续沿用现有职责链：

```text
HitComponent -> HurtComponent -> DamageComponent -> Entity reaction/drop
```

- `HitComponent` 描述工具类型和伤害值。
- `HurtComponent` 判断命中是否匹配，并发出受伤信号。
- `DamageComponent` 管理累计伤害和达到上限事件。
- 树、石头等实体响应信号，负责摇晃效果、销毁及生成各自掉落物。

不得在每个可破坏对象中重新实现命中类型判断或累计伤害逻辑。发现多个实体重复相同反应流程时，应进一步提取可破坏对象组件，而不是继续复制脚本。

## 6. 角色与状态机

- 通用状态基类和状态机保留在 `scripts/state_machine/`。
- 玩家或其他角色的具体状态脚本保留在各自角色目录。
- 状态作为角色场景的子节点组合，不把所有状态逻辑堆入角色主脚本。
- 每个状态只负责本状态的输入、移动、动画、进入、退出和转换判断。
- 状态切换统一通过状态信号交给状态机，状态之间不得直接修改状态机内部字段。
- 物理移动和碰撞相关逻辑在物理回调中执行；普通逐帧表现逻辑才放在进程回调中。
- 新增状态时必须在角色场景中明确配置依赖，并验证进入、退出和返回状态路径。

目标状态接口统一使用正确拼写：

```gdscript
func _on_process(delta: float) -> void
func _on_physics_process(delta: float) -> void
func _on_next_transitions() -> void
func _on_enter() -> void
func _on_exit() -> void
```

现有状态机接口若仍含拼写错误，只有在任务涉及该接口时才修正；修正时必须在同一变更中同步更新基类、状态机调用方、全部状态实现和场景引用，禁止只修改其中一处。

## 7. GDScript 风格

### 7.1 命名

- 文件、目录、变量、函数、信号和 InputMap action：英文 `snake_case`。
- `class_name`、自定义类型和场景根节点：英文 `PascalCase`。
- 常量及新枚举成员：英文 `UPPER_SNAKE_CASE`。
- 私有实现可使用前导下划线；Godot 生命周期函数和信号回调保留规范前导下划线。
- 名称必须表达职责，禁止继续引入 `animiated`、`sprited`、`physyics` 等错误拼写。

旧枚举成员只在相关任务中渐进迁移。重命名枚举成员时必须更新全部引用，并保持现有枚举数值顺序，避免破坏 `.tscn` 中序列化的整数值。

### 7.2 格式和文件顺序

- 使用 Tab 缩进，不混用空格缩进。
- 字符串统一使用双引号。
- 文件末尾保留一个换行，不保留尾随空格。
- 脚本内容按以下顺序组织：`class_name`、`extends`、信号、枚举和常量、`@export`、普通字段、`@onready`、生命周期函数、公共方法、信号回调、私有方法。
- 删除无意义的空 `_ready()`、`_process()` 和模板注释；抽象基类中的空虚方法可以保留。
- 相关逻辑之间保留一个空行，不使用大量空行分隔。

### 7.3 类型

- 函数参数和返回值必须显式标注类型，包括 `void`。
- `@export`、成员变量、信号参数、集合和预加载资源必须尽可能显式标注类型。
- 使用具体组件类型代替宽泛的 `Node`、`Node2D` 或未类型化变量。
- 从碰撞回调接收宽泛类型时，必须先用 `is` 或安全转换验证，再访问组件字段。
- 不为绕过类型错误而移除类型；应修正依赖、节点类型或接口。

### 7.4 信号与依赖

- 跨组件、实体和交互边界优先使用信号，避免组件直接控制所属实体。
- 信号必须表达已发生的事件，并在有数据时声明类型参数。
- 代码连接信号时使用类型安全形式：`component.signal_name.connect(callback)`。
- 回调名称使用 `_on_<source>_<signal>()`；如果是实体的领域反应，可使用清晰的动词名称，但同一脚本内必须一致。
- `@onready` 只用于获取自身场景中的稳定子节点；可替换节点或外部依赖使用 `@export`。

### 7.5 输入、物理和动画

- 输入必须通过 `project.godot` 中的 InputMap action 读取，不硬编码按键。
- 二维移动优先使用 `Input.get_vector()`；若设计明确限制四方向，必须在代码或状态规则中体现并说明优先级。
- `CharacterBody2D.velocity`、`move_and_slide()` 及碰撞状态变更应在物理流程中处理。
- 禁止用切换到无关碰撞层的方式模拟禁用碰撞；应明确调整正确的 layer/mask 或延迟设置碰撞形状。
- 动画名集中按行为和方向组成，例如 `walk_left`、`idle_front`；重复的方向到动画映射应提取为共享函数或状态基类能力。

### 7.6 注释、日志与错误处理

- 标识符使用英文；必要注释和项目文档使用中文。
- 注释解释设计原因、约束或非显然行为，不逐行复述代码。
- 禁止保留 Godot 自动生成的模板注释。
- 禁止提交只用于临时观察流程的 `print()`。
- 可恢复的异常情况使用 `push_warning()`，会破坏运行流程的配置错误使用 `push_error()` 或断言，并提供明确上下文。
- 对可能为空的节点、转换结果、材质或资源先验证，再调用其属性和方法。

## 8. Godot 场景与资源安全

- `.tscn`、`.tres` 只允许进行范围清晰、可人工审查的局部文本修改。
- 禁止手工修改 `uid="..."`、`.uid` 文件内容、`.import` 文件和 `.godot/` 缓存。
- 禁止无必要重排或重写大段 Godot 序列化资源、SpriteFrames、TileSet 或动画数据。
- 移动或重命名资源时优先通过 Godot 编辑器执行；若必须文本修改，必须同步更新所有 `res://` 引用并进行全仓库检查。
- 修改节点名称或层级前必须搜索 NodePath、信号连接、动画轨道和脚本 `$NodePath` 引用。
- 新增碰撞体时必须核对 `project.godot` 中命名的物理层，并明确设置 layer 和 mask。
- 新增场景实例时必须确认所有外部资源存在，脚本类型与根节点类型匹配。
- 不提交 `.godot/`、临时导出目录或本机缓存。

## 9. 新增角色、物体和能力的流程

### 9.1 新角色

1. 在 `scenes/characters/<character_name>/` 创建独立角色场景。
2. 参考现有 Player 的场景组合方式，但只加入该角色需要的能力。
3. 将角色专属状态放在同一目录，将通用状态机能力放在 `scripts/state_machine/`。
4. 通过组件获得命中、受伤、交互或收集能力，不在角色脚本中复制组件逻辑。
5. 在 `scenes/test/` 创建或扩展最小测试场景。

### 9.2 新世界物体或掉落物

1. 在 `scenes/objects/<category>/` 创建可独立实例化的场景。
2. 组合已有组件；若所需行为已在其他实体出现，不得再次复制实现。
3. 实体脚本只处理专属视觉反馈、掉落配置和生命周期。
4. 若会在运行时生成，使用带类型的 `PackedScene` 引用。
5. 验证对象能被单独放入测试场景，并正确处理碰撞、信号和销毁。

### 9.3 新公共能力

1. 先确认该行为已被至少两个实体需要，或具有明确独立职责和复用计划。
2. 在 `scenes/components/` 创建组件脚本；需要节点、碰撞形状或默认配置时同时创建组件场景。
3. 使用导出属性定义输入配置，使用信号定义输出事件。
4. 将已有调用方迁移到组件，不能保留两套并行实现。
5. 分别在至少两个调用方或专用测试场景中验证组件行为。

## 10. 渐进修改与变更边界

- 新文件必须完全符合本规范。
- 修改旧文件时，应修正本次触及代码附近的拼写、类型、无效注释和格式问题。
- 不得为了风格统一批量改动与需求无关的脚本、场景、节点名或资源路径。
- 公共类名、信号、枚举、状态接口或资源路径的变更必须先搜索全部使用点，并在同一变更中原子完成。
- 不得擅自改变游戏数值、输入体验、碰撞语义、动画时序或场景入口。
- 禁止使用 `git reset --hard`、强制检出或其他会丢失用户修改的操作。
- 除非用户明确要求，不创建提交、不推送远端、不修改主场景、不批量导入或删除资源。

## 11. 验证与完成标准

每次修改后至少执行：

1. 检查版本差异，确认只有任务相关文件发生变化。
2. 检查新增或修改的所有 `res://` 路径均存在。
3. 检查 NodePath、信号方法、动画名、InputMap action 和碰撞层配置一致。
4. 检查 GDScript 的类型、回调拼写、未使用参数和潜在空引用。
5. 检查没有新增临时 `print()`、模板注释、空回调或重复实现。
6. Godot CLI 可用时执行无界面项目加载或等价检查，并报告错误和警告。
7. Godot CLI 不可用时执行静态引用与差异检查，明确说明未完成引擎验证，并提供编辑器中的手工验证步骤。

涉及对应系统时必须回归以下场景：

- **角色状态**：Idle 与 Walk 正常切换；工具状态能进入、播放、退出并返回稳定状态；状态切换后碰撞体状态正确。
- **可破坏对象**：只有匹配工具产生伤害；累计伤害达到上限时只触发一次销毁；掉落物生成在正确位置和父节点下。
- **交互设施**：进入和离开检测区域时信号各触发一次；视觉状态与碰撞状态一致。
- **收集物**：仅目标角色可收集；收集事件、数据和销毁顺序正确；不会重复收集。
- **独立场景**：新角色或物体能在最小测试场景中单独实例化，不依赖未声明的外部节点。

最终答复必须说明：

- 实际修改了什么。
- 执行了哪些验证及结果。
- 哪些验证因环境限制未执行。
- 是否保留了用户原有未提交修改。
